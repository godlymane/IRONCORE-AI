/**
 * @deprecated — Import from '../utils/constants' instead.
 * This file re-exports everything from the canonical constants module
 * to avoid breaking existing imports. New code should use utils/constants.
 */
export {
    EXERCISE_DB,
    LEVELS,
    NUTRITION_DEFAULTS,
    getNutritionGoals,
    DEFAULT_MET,
} from '../utils/constants';
